import java.util.Comparator;
import Sport.Ball;

public class OrderbySize implements Comparator<Ball> {
  @Override
  public int compare(Ball b1, Ball b2){
    return b1.getSize() - (descending)
    //b1(-1) vs b2(1)
    if (b1.getSize()= Size.10.9)
    return -1; 
    if (b2.getSize()= Size.)
    return 1; 
    
  }
}
